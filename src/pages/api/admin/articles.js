import { NextApiRequest, NextApiResponse } from 'next';

export default function handler(req: NextApiRequest, res: NextApiResponse) {
    // لوجیک برای لیست مقالات (GET) و ایجاد مقالات (POST) اینجا قرار می‌گیرد
}
